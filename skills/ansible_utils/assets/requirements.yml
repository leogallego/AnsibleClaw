---
# requirements.yml -- Collection dependencies for ansible.utils (collection)
# Generated by AnsibleClaw
#
# Install with: ansible-galaxy collection install -r assets/requirements.yml
#
collections:
  - name: ansible.utils
